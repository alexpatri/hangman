#ifndef UTILS_
#define UTILS_

#include <string>
#include <vector>

namespace utils {
std::vector<std::string> split_string(const std::string &, const std::string &);
} // namespace utils

#endif // !UTILS_
